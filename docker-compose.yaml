version: '3.9'
services:
  redis:
    image: redis:alpine
    container_name: baram-script-redis
    ports:
      - '16379:6379'
    networks:
      - baram-script-network

networks:
  baram-script-network:
    driver: bridge
